author: ${author}
name: ${name}
coreApi: ${coreApi}
version: ${version}

### =============================================== ###

main: "tw.xinshou.discord.plugin.api.audioapi.Event"

require_intents:
  - "GUILD_VOICE_STATES"

require_cache_flags:
  - "VOICE_STATE"

require_member_cache_policies:
  - "VOICE"