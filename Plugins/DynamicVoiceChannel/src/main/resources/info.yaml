author: ${author}
name: ${name}
coreApi: ${coreApi}
version: ${version}

### =============================================== ###

main: "tw.xinshou.discord.plugin.dynamicvoicechannel.Event"

depend_plugins:
  - "SQLiteAPI"

require_intents:
  - "GUILD_VOICE_STATES"

require_cache_flags:
  - "VOICE_STATE"

require_member_cache_policies:
  - "VOICE"